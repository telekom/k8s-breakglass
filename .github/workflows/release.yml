# SPDX-FileCopyrightText: 2025 Deutsche Telekom AG
#
# SPDX-License-Identifier: Apache-2.0

name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: read

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  validate-tag:
    runs-on: ubuntu-latest
    permissions:
      checks: read
      contents: read
    outputs:
      stable: ${{ steps.classify.outputs.stable }}
      prerelease: ${{ steps.classify.outputs.prerelease }}
      make_latest: ${{ steps.classify.outputs.make_latest }}
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Require tag on main branch
        run: |
          if ! git branch -r --contains HEAD | grep -q 'origin/main'; then
            echo "::error::Tag ${GITHUB_REF_NAME} is not on the main branch. Refusing to release."
            exit 1
          fi

      - name: Verify CI passed for commit
        env:
          GH_TOKEN: ${{ github.token }}
        run: hack/verify-commit-check-runs.sh

      - name: Require version tag ref
        id: classify
        run: |
          if [[ "${GITHUB_REF}" =~ ^refs/tags/v(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)(-[0-9A-Za-z.-]+)?$ ]]; then
            echo "Release ref is ${GITHUB_REF}"
          else
            echo "::error::Release workflow must run from a semver tag like v1.2.3 or v1.2.3-rc.1. Build metadata tags are not supported because Docker image tags cannot contain '+'. Got ${GITHUB_REF}"
            exit 1
          fi
          if [[ "${GITHUB_REF_NAME}" =~ ^v(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)$ ]]; then
            echo "stable=true" >> "$GITHUB_OUTPUT"
            echo "prerelease=false" >> "$GITHUB_OUTPUT"
            echo "make_latest=true" >> "$GITHUB_OUTPUT"
          else
            echo "stable=false" >> "$GITHUB_OUTPUT"
            echo "prerelease=true" >> "$GITHUB_OUTPUT"
            echo "make_latest=false" >> "$GITHUB_OUTPUT"
          fi

  # ---------------------------------------------------------------------------
  # Build release manifests, Helm chart package, and bgctl CLI binaries
  # ---------------------------------------------------------------------------
  prepare:
    needs: [validate-tag]
    permissions:
      contents: read
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
          cache: true
      - name: Set up Helm
        uses: azure/setup-helm@9bc31f4ebc9c6b171d7bfbaa5d006ae7abdb4310 # v5.0.1
        with:
          version: v4.2.2
      - name: Generate manifests
        run: |
          make manifests
          make kustomize

          # Set the correct image tag for this release
          VERSION=${{ github.ref_name }}
          cd config/deployment
          ../../bin/kustomize edit set image breakglass=ghcr.io/telekom/k8s-breakglass:${VERSION}
          cd ../..

          # Build complete deployment manifests for each target
          ./bin/kustomize build config/base > manifests-base.yaml
          ./bin/kustomize build config/debug > manifests-debug.yaml
          # Create CRDs-only manifest
          ./bin/kustomize build config/crd > manifests-crds.yaml

          # Verify image tag is correct in output
          echo "Verifying image tags in manifests..."
          grep -o "ghcr.io/telekom/k8s-breakglass:${VERSION}" manifests-base.yaml || { echo "ERROR: Image tag not set correctly"; exit 1; }

          # Package everything
          tar -czf manifests-${{ github.ref_name }}.tgz \
            config/crd/bases \
            manifests-base.yaml \
            manifests-debug.yaml \
            manifests-crds.yaml
      - name: Upload manifests artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: manifests
          path: |
            manifests-${{ github.ref_name }}.tgz
            manifests-base.yaml
            manifests-debug.yaml
            manifests-crds.yaml
      - name: Package escalation Helm chart
        run: |
          set -euo pipefail

          # Keep chart version from Chart.yaml; stamp appVersion with the release tag.
          mkdir -p chart-dist
          helm package charts/escalation-config \
            --destination chart-dist \
            --app-version "${{ github.ref_name }}"
          # The standalone catalogue is introduced by the foundation stack.
          # Keep the base release valid before that chart lands, while making
          # its package part of the same immutable release once present.
          if [ -d charts/debug-session-catalogue ]; then
            helm package charts/debug-session-catalogue \
              --destination chart-dist \
              --app-version "${{ github.ref_name }}"
          fi

          echo "Packaged chart artifacts:"
          ls -la chart-dist
      - name: Upload chart artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-chart
          path: chart-dist/*.tgz
      - name: Build bgctl release assets
        run: |
          set -e
          VERSION=${{ github.ref_name }}
          GIT_COMMIT=${{ github.sha }}
          BUILD_DATE=${{ github.event.head_commit.timestamp }}
          mkdir -p dist
          platforms="linux/amd64 linux/arm64 darwin/amd64 darwin/arm64 windows/amd64"
          for platform in $platforms; do
            os=${platform%/*}
            arch=${platform#*/}
            bin=bgctl
            ext=""
            if [ "$os" = "windows" ]; then
              ext=".exe"
            fi
            outdir="dist/bgctl_${os}_${arch}"
            mkdir -p "$outdir"
            GOOS=$os GOARCH=$arch CGO_ENABLED=0 go build -trimpath \
              -ldflags "-s -w -X github.com/telekom/k8s-breakglass/pkg/version.Version=$VERSION -X github.com/telekom/k8s-breakglass/pkg/version.GitCommit=$GIT_COMMIT -X github.com/telekom/k8s-breakglass/pkg/version.BuildDate=$BUILD_DATE" \
              -o "$outdir/$bin$ext" ./cmd/bgctl
            if [ "$os" = "windows" ]; then
              (cd dist && zip -r "bgctl_${os}_${arch}.zip" "bgctl_${os}_${arch}")
              (cd dist && shasum -a 256 "bgctl_${os}_${arch}.zip" > "bgctl_${os}_${arch}.zip.sha256")
            else
              (cd dist && tar -czf "bgctl_${os}_${arch}.tar.gz" "bgctl_${os}_${arch}")
              (cd dist && shasum -a 256 "bgctl_${os}_${arch}.tar.gz" > "bgctl_${os}_${arch}.tar.gz.sha256")
            fi
          done
          # Generate combined checksums file (only archive files, not directories)
          (cd dist && shasum -a 256 *.tar.gz *.zip > "bgctl_${VERSION}_checksums.txt")
      - name: Upload bgctl artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: bgctl-dist
          path: |
            dist/bgctl_*.tar.gz
            dist/bgctl_*.zip
            dist/bgctl_*.sha256
            dist/bgctl_${{ github.ref_name }}_checksums.txt

  # ---------------------------------------------------------------------------
  # Build per-architecture container images natively (no QEMU emulation)
  # ---------------------------------------------------------------------------
  build-image:
    needs: [validate-tag]
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux/amd64
            runner: ubuntu-latest
          - platform: linux/arm64
            # NOTE: ubuntu-24.04-arm is a GitHub-hosted larger runner with different billing.
            # See: https://docs.github.com/en/actions/using-github-hosted-runners/using-larger-runners
            runner: ubuntu-24.04-arm
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Login to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Set up Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0
      - name: Set build timestamp
        id: build-time
        env:
          HEAD_COMMIT_TIMESTAMP: ${{ github.event.head_commit.timestamp }}
        run: |
          set -euo pipefail
          BUILD_DATE="${HEAD_COMMIT_TIMESTAMP:-}"
          if [ -z "${BUILD_DATE}" ]; then
            BUILD_DATE="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          fi
          echo "build-date=${BUILD_DATE}" >> "$GITHUB_OUTPUT"
      - name: Docker meta
        id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6.2.0
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          labels: |
            org.opencontainers.image.title=Kubernetes Breakglass
            org.opencontainers.image.description=Secure, auditable privilege escalation system for Kubernetes clusters with real-time webhook integration and time-bounded access
            org.opencontainers.image.url=https://github.com/telekom/k8s-breakglass
            org.opencontainers.image.documentation=https://github.com/telekom/k8s-breakglass/tree/main/docs
            org.opencontainers.image.source=https://github.com/telekom/k8s-breakglass
            org.opencontainers.image.licenses=Apache-2.0
            org.opencontainers.image.vendor=Deutsche Telekom
            org.opencontainers.image.version=${{ github.ref_name }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.created=${{ steps.build-time.outputs.build-date }}
            com.github.repo.branch=${{ github.ref_name }}
            com.github.repo.owner=${{ github.repository_owner }}
            com.buildkit.build.ref=${{ github.ref }}
      - name: Build & push single-platform image (by digest)
        id: build
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: .
          push: true
          # Push by digest only — no tag yet; tags are created in the assemble step
          platforms: ${{ matrix.platform }}
          labels: ${{ steps.meta.outputs.labels }}
          # Note: buildx cache (cache-from/cache-to) is intentionally omitted for
          # release builds to ensure clean, reproducible images without layer reuse
          # from development iterations.
          build-args: |
            VERSION=${{ github.ref_name }}
            GIT_COMMIT=${{ github.sha }}
            BUILD_DATE=${{ steps.build-time.outputs.build-date }}
          outputs: type=image,name=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }},push-by-digest=true,name-canonical=true,push=true
      - name: Export digest
        id: set-digest
        run: |
          PLATFORM="${{ matrix.platform }}"
          ARCH_FULL=$(echo "${PLATFORM}" | tr '/' '-')  # e.g., linux-amd64 or linux-arm64
          ARCH_SHORT="${PLATFORM#*/}"  # e.g., amd64 or arm64
          if [ -z "${ARCH_SHORT}" ] || [ "${ARCH_SHORT}" = "${PLATFORM}" ]; then
            echo "Invalid platform format: '${PLATFORM}'. Expected '<os>/<arch>' (e.g., 'linux/amd64')." >&2
            exit 1
          fi
          DIGEST="${{ steps.build.outputs.digest }}"
          if [ -z "${DIGEST}" ]; then
            echo "Error: Build produced no digest for platform '${PLATFORM}' (arch=${ARCH_SHORT})." >&2
            exit 1
          fi
          echo "digest-${ARCH_SHORT}=${DIGEST}" >> "$GITHUB_OUTPUT"
          echo "artifact-name=digest-${ARCH_SHORT}" >> "$GITHUB_OUTPUT"
          echo "arch-full=${ARCH_FULL}" >> "$GITHUB_OUTPUT"
          # Also persist to a file artifact so the assemble job can collect all digests
          mkdir -p /tmp/digests
          echo "${DIGEST}" > "/tmp/digests/${ARCH_FULL}"
      - name: Upload digest
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ${{ steps.set-digest.outputs.artifact-name }}
          path: /tmp/digests/${{ steps.set-digest.outputs.arch-full }}

  # ---------------------------------------------------------------------------
  # Assemble per-arch images into a single multi-arch manifest and tag it
  # ---------------------------------------------------------------------------
  assemble:
    needs: [build-image, validate-tag]
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write
    runs-on: ubuntu-latest
    outputs:
      digest: ${{ steps.inspect.outputs.digest }}
    steps:
      - name: Download all digests
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: digest-*
          merge-multiple: true
          path: /tmp/digests
      - name: Login to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Set up Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0
      - name: Docker meta
        id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6.2.0
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=${{ github.ref_name }}
            type=raw,value=latest,enable=${{ needs.validate-tag.outputs.stable == 'true' }}
      - name: Create and push multi-arch manifest
        env:
          REGISTRY: ${{ env.REGISTRY }}
          IMAGE_NAME: ${{ env.IMAGE_NAME }}
        run: |
          set -euo pipefail

          # Verify digest files exist
          if [ -z "$(ls -A /tmp/digests 2>/dev/null)" ]; then
            echo "Error: No digest files found in /tmp/digests. Artifact download may have failed." >&2
            exit 1
          fi

          # Verify all expected platform digests are present
          for expected in linux-amd64 linux-arm64; do
            if [ ! -f "/tmp/digests/${expected}" ]; then
              echo "Error: Missing digest for platform ${expected}. Cannot assemble incomplete multi-arch manifest." >&2
              exit 1
            fi
          done

          # Display discovered digest files for diagnostics
          echo "Digest files found:"
          ls -la /tmp/digests/

          # Collect per-arch digests into an array
          DIGESTS=()
          for f in /tmp/digests/*; do
            d=$(cat "$f")
            echo "  $(basename "$f"): ${d}"
            DIGESTS+=("${REGISTRY}/${IMAGE_NAME}@${d}")
          done
          echo "Assembling multi-arch manifest from ${#DIGESTS[@]} platform(s): ${DIGESTS[*]}"

          # Build the --tag flags from metadata into an array
          TAGS=()
          while IFS= read -r tag; do
            # Skip empty lines that can result from trailing newlines
            [ -z "${tag}" ] && continue
            TAGS+=("--tag" "$tag")
          done <<< "${{ steps.meta.outputs.tags }}"

          # Ensure we have at least one tag
          if [ ${#TAGS[@]} -eq 0 ]; then
            echo "Error: No tags were generated from docker/metadata-action; aborting multi-arch manifest creation." >&2
            exit 1
          fi

          # Create and push the multi-arch manifest list
          docker buildx imagetools create "${TAGS[@]}" "${DIGESTS[@]}"
      - name: Inspect final image
        id: inspect
        run: |
          set -euo pipefail
          IMG="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.ref_name }}"

          INSPECT_OUTPUT=""
          DIGEST=""
          for i in $(seq 1 30); do
            if INSPECT_OUTPUT="$(docker buildx imagetools inspect "${IMG}" 2>/tmp/imagetools-inspect.err)"; then
              DIGEST="$(printf '%s\n' "${INSPECT_OUTPUT}" | awk '/^Digest:[[:space:]]+sha256:[0-9a-f]{64}$/ {print $2; exit}')"
              if [ -n "${DIGEST}" ]; then
                echo "Registry digest resolved after ${i} attempt(s)"
                break
              fi
              echo "Attempt ${i}/30: manifest is available but no registry digest was reported, retrying in 10s..."
            else
              echo "Attempt ${i}/30: manifest not available yet, retrying in 10s..."
              cat /tmp/imagetools-inspect.err || true
            fi
            sleep 10
          done

          if [ -z "${DIGEST}" ]; then
            echo "::error::Could not determine registry digest for ${IMG}; provenance signing and attestations would have an unverifiable subject."
            exit 1
          fi

          echo "${INSPECT_OUTPUT}"
          echo "digest=${DIGEST}" >> "$GITHUB_OUTPUT"
          echo "Multi-arch manifest digest: ${DIGEST}"
      - name: Attest provenance
        uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8 # v4.2.2
        with:
          subject-name: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          subject-digest: ${{ steps.inspect.outputs.digest }}
          push-to-registry: true
      - name: Install cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2
      - name: Sign container image (keyless)
        run: |
          set -euo pipefail
          cosign sign --yes ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ steps.inspect.outputs.digest }}
      - name: Generate SBOM for attestation
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0
        with:
          image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ steps.inspect.outputs.digest }}
          format: spdx-json
          output-file: sbom.spdx.json
          upload-release-assets: false
      - name: Attest SBOM (keyless)
        run: |
          cosign attest --yes --type spdxjson --predicate sbom.spdx.json \
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ steps.inspect.outputs.digest }}

  # ---------------------------------------------------------------------------
  # Push multi-arch image to Artifactory OCI mirror
  # ---------------------------------------------------------------------------
  artifactory:
    needs: [assemble]
    continue-on-error: true  # Artifactory mirror is best-effort; must not block releases
    permissions:
      packages: read
      id-token: write  # Required for cosign keyless signing/copy
    runs-on: ubuntu-latest
    steps:
      - name: Login to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Set up Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0
      - name: Install cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2
      - name: Tag and push image to Artifactory OCI
        env:
          ARTIFACTORY_URL: artifactory.devops.telekom.de
          ARTIFACTORY_REPO: cit-t-caas-oci/images/t-caas
          REGISTRY: ${{ env.REGISTRY }}
          IMAGE_NAME: ${{ env.IMAGE_NAME }}
        run: |
          set -euo pipefail
          # docker CLI login using environment secrets
          echo "Logging in to Artifactory OCI at ${ARTIFACTORY_URL}"
          echo "${{ secrets.AF_TOKEN }}" | docker login --username ${{ secrets.AF_USER }} --password-stdin https://${ARTIFACTORY_URL}

          # Diagnose Artifactory repo accessibility
          REPO_KEY=$(echo "${ARTIFACTORY_REPO}" | cut -d'/' -f1)
          echo "Checking Artifactory storage API for repo key: ${REPO_KEY}"
          HTTP_STATUS=$(curl -s -o /dev/null -w "%{http_code}" -u "${{ secrets.AF_USER }}:${{ secrets.AF_TOKEN }}" "https://${ARTIFACTORY_URL}/artifactory/api/storage/${REPO_KEY}" ) || true
          echo "Artifactory storage API HTTP status: ${HTTP_STATUS}"
          if [ "${HTTP_STATUS}" != "200" ]; then
            echo "Warning: Artifactory repo ${REPO_KEY} not accessible (HTTP ${HTTP_STATUS}). Ensure AF_USER/AF_TOKEN are valid and have deploy permissions."
          fi

          IMAGE=${{ env.IMAGE_NAME }}
          TAG=${{ github.ref_name }}
          SRC=${REGISTRY}/${IMAGE}:${TAG}
          DST=${ARTIFACTORY_URL}/${ARTIFACTORY_REPO}/${IMAGE}:${TAG}

          echo "Copying multi-arch manifest ${SRC} -> ${DST}"

          # Use buildx imagetools to copy the full multi-arch manifest (all platforms)
          docker buildx imagetools create --tag "${DST}" "${SRC}"

          # Copy cosign signatures and attestations to Artifactory
          # Cosign artifacts (signatures, SBOM attestations) are stored as OCI referrers
          # alongside the image in GHCR. docker buildx imagetools does not copy these,
          # so we use cosign copy to replicate them to the Artifactory mirror.
          # Use digest-based source reference to ensure we copy artifacts for the exact
          # signed manifest, not whatever the tag resolves to at copy time.
          SRC_DIGEST=${REGISTRY}/${IMAGE}@${{ needs.assemble.outputs.digest }}
          echo "Copying cosign artifacts (signatures + attestations) to Artifactory"
          # Set explicit registry credentials for cosign — docker login credentials
          # are not always reused by cosign for the destination registry.
          export COSIGN_REPOSITORY="${DST}"
          cosign copy "${SRC_DIGEST}" "${DST}" --only=sig,att || echo "::warning::cosign copy failed — Artifactory image will not be verifiable via cosign"

          # Verify the pushed image is indexed in Artifactory (retry with backoff)
          CHECK_PATH=$(echo "${ARTIFACTORY_REPO}/${IMAGE}" | sed 's@//*@/@g')
          echo "Checking storage for path: ${CHECK_PATH}"
          for attempt in 1 2 3; do
            sleep $((attempt * 2))
            HTTP_STATUS_AFTER=$(curl -s -o /dev/null -w "%{http_code}" -u "${{ secrets.AF_USER }}:${{ secrets.AF_TOKEN }}" "https://${ARTIFACTORY_URL}/artifactory/api/storage/${CHECK_PATH}?list" ) || true
            echo "Attempt ${attempt}: HTTP status ${HTTP_STATUS_AFTER}"
            if [ "${HTTP_STATUS_AFTER}" = "200" ]; then
              echo "Artifact path appears present in Artifactory."
              break
            fi
          done
          if [ "${HTTP_STATUS_AFTER}" != "200" ]; then
            echo "Warning: Artifact path not confirmed after retries (HTTP ${HTTP_STATUS_AFTER}). It might be a permission issue or Artifactory indexing delay."
          fi

  # ---------------------------------------------------------------------------
  # Publish Helm charts to GHCR OCI; the following step signs and verifies
  # every packaged chart, including the optional standalone catalogue.
  # ---------------------------------------------------------------------------
  publish-chart:
    needs: [prepare]
    permissions:
      contents: read
      packages: write
      id-token: write
    runs-on: ubuntu-latest
    steps:
      - name: Download chart artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-chart
          path: chart-dist
      - name: Set up Helm
        uses: azure/setup-helm@9bc31f4ebc9c6b171d7bfbaa5d006ae7abdb4310 # v5.0.1
        with:
          version: v4.2.2
      - name: Set up Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0
      - name: Install Cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2
        with:
          cosign-release: v3.0.2
      - name: Generate chart SBOM
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0
        with:
          path: chart-dist
          format: spdx-json
          output-file: chart-sbom.spdx.json
      - name: Login to GHCR for Helm OCI
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | helm registry login ${{ env.REGISTRY }} \
            --username "${{ github.actor }}" \
            --password-stdin
      - name: Publish charts
        env:
          CHART_REPO: oci://ghcr.io/${{ github.repository }}/charts
        run: |
          set -euo pipefail
          hack/publish-helm-charts.sh chart-dist "${CHART_REPO}" "${{ github.ref_name }}"

      - name: Sign and verify immutable chart contracts
        env:
          CHART_REPO: oci://ghcr.io/${{ github.repository }}/charts
          SUPPLY_CHAIN_IDENTITY_REGEXP: https://github.com/telekom/k8s-breakglass/.github/workflows/release.yml@refs/tags/v[0-9].*
        run: |
          set -euo pipefail
          shopt -s nullglob
          chart_packages=(chart-dist/*.tgz)
          [ "${#chart_packages[@]}" -gt 0 ] || { echo "No chart packages found" >&2; exit 1; }
          : > catalogue-image-refs.txt
          catalogue_chart_ref=""
          for chart_package in "${chart_packages[@]}"; do
            metadata="$(helm show chart "${chart_package}")"
            chart_name="$(printf '%s\n' "${metadata}" | awk '/^name:/ {print $2}')"
            chart_version="$(printf '%s\n' "${metadata}" | awk '/^version:/ {print $2}')"
            remote="${CHART_REPO}/${chart_name}"
            digest="$(docker buildx imagetools inspect "${remote}:${chart_version}" | awk '/^Digest:/ {print $2; exit}')"
            [[ "${digest}" =~ ^sha256:[a-f0-9]{64}$ ]] || { echo "No immutable digest for ${remote}:${chart_version}" >&2; exit 1; }
            subject="${remote#oci://}@${digest}"
            cosign sign --yes "${subject}"
            cosign attest --yes --type spdxjson --predicate chart-sbom.spdx.json "${subject}"
            digest_hex="${digest#sha256:}"
            jq -n --arg subject "${remote#oci://}" --arg digest "${digest_hex}" --arg sha "${GITHUB_SHA}" \
              --arg workflow "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}" \
              '{_type:"https://in-toto.io/Statement/v1",subject:[{name:$subject,digest:{sha256:$digest}}],predicateType:"https://slsa.dev/provenance/v1",predicate:{buildDefinition:{buildType:"https://github.com/telekom/k8s-breakglass/.github/workflows/release.yml",externalParameters:{source:{uri:"https://github.com/telekom/k8s-breakglass",digest:{sha1:$sha}}},resolvedDependencies:[]},runDetails:{builder:{id:$workflow}}}}' > chart-provenance.json
            cosign attest --yes --type slsaprovenance --predicate chart-provenance.json "${subject}"
            cosign verify "${subject}" \
              --certificate-identity-regexp="${SUPPLY_CHAIN_IDENTITY_REGEXP}" \
              --certificate-oidc-issuer="https://token.actions.githubusercontent.com"
            cosign verify-attestation "${subject}" --type spdxjson \
              --certificate-identity-regexp="${SUPPLY_CHAIN_IDENTITY_REGEXP}" \
              --certificate-oidc-issuer="https://token.actions.githubusercontent.com"
            cosign verify-attestation "${subject}" --type slsaprovenance \
              --certificate-identity-regexp="${SUPPLY_CHAIN_IDENTITY_REGEXP}" \
              --certificate-oidc-issuer="https://token.actions.githubusercontent.com"
            if [ "${chart_name}" = "debug-session-catalogue" ]; then
              catalogue_chart_ref="${subject}"
              helm show values "${chart_package}" > catalogue-values.yaml
              ruby -ryaml -e '
                values = YAML.safe_load(File.read(ARGV.fetch(0)), aliases: false) || {}
                images = values.fetch("images", {})
                refs = values.fetch("profiles", []).map do |profile|
                  image = profile["image"] || images.fetch(profile.fetch("imageKey"))
                  repository = image.fetch("repository")
                  digest = image["digest"].to_s
                  tag = image["tag"].to_s
                  digest.empty? ? "#{repository}:#{tag}" : "#{repository}@#{digest}"
                end
                puts refs.uniq.sort
              ' catalogue-values.yaml | awk '/@sha256:[0-9a-f]{64}$/' > catalogue-image-refs.txt
            fi
          done
          if [ -n "${catalogue_chart_ref}" ]; then
            if [ -s catalogue-image-refs.txt ]; then
              hack/verify-catalogue-supply-chain.sh --images-file catalogue-image-refs.txt --chart "${catalogue_chart_ref}"
            else
              echo "No digest-pinned utility images are embedded in chart defaults; verify consumer-provided image digests separately."
              cosign verify "${catalogue_chart_ref}" \
                --certificate-identity-regexp="${SUPPLY_CHAIN_IDENTITY_REGEXP}" \
                --certificate-oidc-issuer="https://token.actions.githubusercontent.com"
            fi
          fi

  # ---------------------------------------------------------------------------
  # Create GitHub Release with manifests, bgctl binaries, and SBOM
  # ---------------------------------------------------------------------------
  release:
    # NOTE: Does not depend on `artifactory` intentionally — Artifactory is a
    # best-effort mirror and should not block GitHub Release creation.
    needs: [prepare, assemble, publish-chart, validate-tag]
    permissions:
      contents: write
      packages: read
    runs-on: ubuntu-latest
    steps:
      - name: Download manifests
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: manifests
          path: .
      - name: Download bgctl dist
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: bgctl-dist
          path: dist
      - name: Download chart artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-chart
          path: chart-dist
      - name: Generate SBOM (Syft)
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0
        with:
          image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.ref_name }}
          format: spdx-json
          output-file: sbom-${{ github.ref_name }}.spdx.json
          upload-release-assets: false
      - name: Generate release asset checksums
        run: |
          set -euo pipefail
          shopt -s nullglob

          chart_packages=(chart-dist/*.tgz)
          bgctl_archives=(dist/bgctl_*.tar.gz dist/bgctl_*.zip)
          if [ "${#chart_packages[@]}" -eq 0 ]; then
            echo "::error::No Helm chart packages found for checksum generation"
            exit 1
          fi
          if [ "${#bgctl_archives[@]}" -eq 0 ]; then
            echo "::error::No bgctl archives found for checksum generation"
            exit 1
          fi

          artifacts=(
            manifests-${{ github.ref_name }}.tgz
            manifests-base.yaml
            manifests-debug.yaml
            manifests-crds.yaml
            "${chart_packages[@]}"
            "${bgctl_archives[@]}"
            sbom-${{ github.ref_name }}.spdx.json
          )
          mapfile -t artifacts < <(printf '%s\n' "${artifacts[@]}" | LC_ALL=C sort)

          if [ "${#artifacts[@]}" -eq 0 ]; then
            echo "::error::No release artifacts found for checksum generation"
            exit 1
          fi

          for artifact in "${artifacts[@]}"; do
            if [ ! -f "${artifact}" ]; then
              echo "::error::Missing release artifact for checksum generation: ${artifact}"
              exit 1
            fi
          done

          checksum_file="release-${{ github.ref_name }}-checksums.txt"
          : > "${checksum_file}"
          for artifact in "${artifacts[@]}"; do
            artifact_dir=$(dirname "${artifact}")
            artifact_name=$(basename "${artifact}")
            if [ "${artifact_dir}" = "." ]; then
              sha256sum "${artifact_name}" >> "${checksum_file}"
            else
              (cd "${artifact_dir}" && sha256sum "${artifact_name}") >> "${checksum_file}"
            fi
          done
      - name: Create GitHub Release
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          prerelease: ${{ needs.validate-tag.outputs.prerelease }}
          make_latest: ${{ needs.validate-tag.outputs.make_latest }}
          files: |
            manifests-${{ github.ref_name }}.tgz
            manifests-base.yaml
            manifests-debug.yaml
            manifests-crds.yaml
            chart-dist/*.tgz
            dist/bgctl_*.tar.gz
            dist/bgctl_*.zip
            dist/bgctl_*.sha256
            dist/bgctl_${{ github.ref_name }}_checksums.txt
            release-${{ github.ref_name }}-checksums.txt
            sbom-${{ github.ref_name }}.spdx.json
          generate_release_notes: true
