apiVersion: v1
kind: Secret
metadata:
  name: tenant-b-kubeconfig-secret-test
  namespace: default
type: Opaque
# stringData is used here so the file does not contain real base64 data.
# The setup script will create proper secrets at runtime. Replace this value if you
# want to bake a kubeconfig into the overlay (not recommended).
stringData:
  kubeconfig: |
    # REPLACE_ME: paste kubeconfig YAML here if you must

---
apiVersion: v1
kind: Secret
metadata:
  name: tenant-kubeconfig-secret-test
  namespace: default
type: Opaque
stringData:
  kubeconfig: |
    # REPLACE_ME: paste kubeconfig YAML here if you must
