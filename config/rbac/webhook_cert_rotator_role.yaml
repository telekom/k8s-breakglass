apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: breakglass-webhook-cert-rotator-role
  namespace: breakglass-system
rules:
# Permissions for cert-controller to manage webhook certificates
# Following principle of least privilege: minimal permissions for necessary operations

# Create secrets only once at initialization (on the namespace)
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - create
  - list
  - watch
# Update and patch the specific breakglass-webhook-certs secret
- apiGroups:
  - ""
  resources:
  - secrets
  resourceNames:
  - breakglass-webhook-certs
  verbs:
  - get
  - update
  - patch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: breakglass-webhook-cert-rotator-role-binding
  namespace: breakglass-system
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: breakglass-webhook-cert-rotator-role
subjects:
- kind: ServiceAccount
  name: breakglass-manager
  namespace: breakglass-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: breakglass-webhook-cert-rotator-cluster-role
rules:
# List and watch all Secrets (needed for cert-rotator's cache sync)
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - list
  - watch
  - get
# List and watch all ValidatingWebhookConfigurations to discover and monitor them
- apiGroups:
  - admissionregistration.k8s.io
  resources:
  - validatingwebhookconfigurations
  verbs:
  - list
  - watch
  - get
# Patch each specific ValidatingWebhookConfiguration to inject CA bundle
- apiGroups:
  - admissionregistration.k8s.io
  resources:
  - validatingwebhookconfigurations
  resourceNames:
  - breakglass-validating-session-webhook
  - breakglass-validating-escalation-webhook
  - breakglass-validating-clusterconfig-webhook
  - breakglass-validating-identityprovider-webhook
  verbs:
  - get
  - patch
  - update
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: breakglass-webhook-cert-rotator-cluster-role-binding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: breakglass-webhook-cert-rotator-cluster-role
subjects:
- kind: ServiceAccount
  name: breakglass-manager
  namespace: breakglass-system
