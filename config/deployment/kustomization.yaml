apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- app.yaml
- service.yaml
- ingress.yaml
- namespace.yaml

# Image configuration - use GHCR registry
# Tag is set via kustomize edit during release/deploy
images:
- name: breakglass
  newName: ghcr.io/telekom/k8s-breakglass
  newTag: latest
