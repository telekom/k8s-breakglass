apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# DEPRECATED: This target is deprecated. Use config/base instead.
# This is kept for backward compatibility with existing documentation and scripts.
# config/base is the new canonical production deployment target.

resources:
- ../base
