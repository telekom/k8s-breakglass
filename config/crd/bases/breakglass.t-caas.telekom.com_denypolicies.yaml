---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: denypolicies.breakglass.t-caas.telekom.com
spec:
  group: breakglass.t-caas.telekom.com
  names:
    kind: DenyPolicy
    listKind: DenyPolicyList
    plural: denypolicies
    shortNames:
    - dpolicy
    singular: denypolicy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.precedence
      name: Precedence
      type: integer
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DenyPolicy is the Schema for the denypolicies API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DenyPolicySpec defines deny rules applicable to sessions
              / clusters / tenants.
            properties:
              appliesTo:
                description: |-
                  appliesTo scopes the policy. Empty means global.
                  Any listed selector must match (logical AND within struct, lists are OR).
                properties:
                  clusters:
                    description: clusters list exact clusterIDs.
                    items:
                      type: string
                    type: array
                  sessions:
                    description: sessions list specific BreakglassSession names.
                    items:
                      type: string
                    type: array
                  tenants:
                    description: tenants list tenant IDs.
                    items:
                      type: string
                    type: array
                type: object
              podSecurityRules:
                description: |-
                  podSecurityRules evaluates pod specifications for exec/attach/portforward requests.
                  When a user attempts to exec into a pod, the pod's security context is analyzed
                  and a risk score is calculated based on configured risk factors.
                properties:
                  appliesTo:
                    description: |-
                      appliesTo specifies which pod subresources trigger this evaluation.
                      Default: exec, attach, portforward
                    properties:
                      subresources:
                        description: subresources to evaluate. If empty, defaults
                          to ["exec", "attach", "portforward"].
                        items:
                          type: string
                        type: array
                    type: object
                  blockFactors:
                    description: |-
                      blockFactors immediately deny if ANY listed factor is present, regardless of score.
                      Valid values: hostNetwork, hostPID, hostIPC, privilegedContainer, hostPathWritable, runAsRoot
                    items:
                      type: string
                    type: array
                  exemptions:
                    description: exemptions exclude certain pods from security evaluation.
                    properties:
                      namespaces:
                        description: |-
                          namespaces to skip evaluation for.
                          Supports pattern matching (glob-style) and label-based namespace selection.
                          Common exemptions: kube-system, monitoring, logging
                        properties:
                          patterns:
                            description: |-
                              patterns contains namespace name patterns (glob-style, legacy format).
                              Supports wildcards: "app-*", "kube-*", "prod-??-*"
                              Empty list matches no namespaces via patterns.
                            items:
                              type: string
                            type: array
                          selectorTerms:
                            description: |-
                              selectorTerms enables label-based namespace selection.
                              When specified, namespaces matching ANY term are included (OR semantics).
                              Each term's matchLabels and matchExpressions are ANDed within the term.
                            items:
                              description: |-
                                NamespaceSelectorTerm represents a single selector term.
                                All requirements within a term are ANDed together.
                                Multiple terms are ORed (like Kubernetes node affinity).
                              properties:
                                matchExpressions:
                                  description: |-
                                    matchExpressions matches namespaces using set-based requirements.
                                    Multiple expressions are ANDed together.
                                  items:
                                    description: |-
                                      NamespaceSelectorRequirement is a selector requirement for namespaces.
                                      Mirrors metav1.LabelSelectorRequirement for consistency with Kubernetes patterns.
                                    properties:
                                      key:
                                        description: key is the label key the selector
                                          applies to.
                                        maxLength: 63
                                        minLength: 1
                                        type: string
                                      operator:
                                        allOf:
                                        - enum:
                                          - In
                                          - NotIn
                                          - Exists
                                          - DoesNotExist
                                        - enum:
                                          - In
                                          - NotIn
                                          - Exists
                                          - DoesNotExist
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators: In, NotIn, Exists, DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values.
                                          Required for In and NotIn operators.
                                          Must be empty for Exists and DoesNotExist.
                                        items:
                                          type: string
                                        type: array
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels matches namespaces with ALL specified labels (AND semantics).
                                    Example: {"environment": "production", "team": "sre"}
                                  type: object
                              type: object
                            type: array
                        type: object
                      podLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          podLabels: pods with ALL specified labels are exempt.
                          Example: {"breakglass.telekom.com/security-exempt": "true"}
                        type: object
                    type: object
                  failMode:
                    default: closed
                    description: |-
                      failMode determines behavior when pod spec cannot be fetched from target cluster.
                      "open" = allow request if fetch fails (fail-open)
                      "closed" = deny request if fetch fails (fail-closed, more secure)
                    enum:
                    - open
                    - closed
                    type: string
                  riskFactors:
                    description: |-
                      riskFactors assigns numeric weights (0-100) to dangerous pod configurations.
                      The total risk score is the sum of all detected factors.
                    properties:
                      capabilities:
                        additionalProperties:
                          type: integer
                        description: |-
                          capabilities maps Linux capability names to risk scores.
                          Example: {"NET_ADMIN": 50, "SYS_ADMIN": 80, "SYS_PTRACE": 60}
                        type: object
                      hostIPC:
                        description: 'hostIPC: pod uses host IPC namespace (can access
                          host shared memory)'
                        type: integer
                      hostNetwork:
                        description: 'hostNetwork: pod uses host network namespace
                          (enables network sniffing, bypasses network policies)'
                        type: integer
                      hostPID:
                        description: 'hostPID: pod uses host PID namespace (can see/signal
                          all host processes)'
                        type: integer
                      hostPathReadOnly:
                        description: 'hostPathReadOnly: pod has read-only hostPath
                          volume mounts (lower risk than writable)'
                        type: integer
                      hostPathWritable:
                        description: 'hostPathWritable: pod has writable hostPath
                          volume mounts'
                        type: integer
                      privilegedContainer:
                        description: 'privilegedContainer: container runs in privileged
                          mode (full host access)'
                        type: integer
                      runAsRoot:
                        description: 'runAsRoot: container runs as root user (UID
                          0)'
                        type: integer
                    type: object
                  thresholds:
                    description: |-
                      thresholds define actions based on cumulative risk score.
                      Evaluated in order; first matching threshold determines the action.
                    items:
                      description: RiskThreshold defines an action to take when risk
                        score falls within a range.
                      properties:
                        action:
                          description: |-
                            action to take when score <= maxScore: "allow", "warn", or "deny"
                            - allow: permit the request silently
                            - warn: permit but log a warning and emit metrics
                            - deny: block the request with reason message
                          enum:
                          - allow
                          - warn
                          - deny
                          type: string
                        maxScore:
                          description: |-
                            maxScore is the upper bound (inclusive) for this threshold.
                            Thresholds are evaluated in order; use ascending maxScore values.
                          type: integer
                        reason:
                          description: |-
                            reason is the message returned to user when action is "deny".
                            Supports Go template variables: {{.Score}}, {{.Factors}}, {{.Pod}}, {{.Namespace}}
                          type: string
                      required:
                      - action
                      - maxScore
                      type: object
                    type: array
                required:
                - riskFactors
                - thresholds
                type: object
              precedence:
                description: precedence (lower wins). If unset defaults to 100.
                format: int32
                type: integer
              rules:
                description: rules are evaluated in order; first matching rule denies.
                items:
                  description: DenyRule blocks an action matching the attributes.
                  properties:
                    apiGroups:
                      description: apiGroups for the resource ("" for core)
                      items:
                        type: string
                      type: array
                    namespaces:
                      description: |-
                        namespaces supports patterns (glob-style) and label-based namespace selection.
                        Empty means cluster-scoped only resources.
                      properties:
                        patterns:
                          description: |-
                            patterns contains namespace name patterns (glob-style, legacy format).
                            Supports wildcards: "app-*", "kube-*", "prod-??-*"
                            Empty list matches no namespaces via patterns.
                          items:
                            type: string
                          type: array
                        selectorTerms:
                          description: |-
                            selectorTerms enables label-based namespace selection.
                            When specified, namespaces matching ANY term are included (OR semantics).
                            Each term's matchLabels and matchExpressions are ANDed within the term.
                          items:
                            description: |-
                              NamespaceSelectorTerm represents a single selector term.
                              All requirements within a term are ANDed together.
                              Multiple terms are ORed (like Kubernetes node affinity).
                            properties:
                              matchExpressions:
                                description: |-
                                  matchExpressions matches namespaces using set-based requirements.
                                  Multiple expressions are ANDed together.
                                items:
                                  description: |-
                                    NamespaceSelectorRequirement is a selector requirement for namespaces.
                                    Mirrors metav1.LabelSelectorRequirement for consistency with Kubernetes patterns.
                                  properties:
                                    key:
                                      description: key is the label key the selector
                                        applies to.
                                      maxLength: 63
                                      minLength: 1
                                      type: string
                                    operator:
                                      allOf:
                                      - enum:
                                        - In
                                        - NotIn
                                        - Exists
                                        - DoesNotExist
                                      - enum:
                                        - In
                                        - NotIn
                                        - Exists
                                        - DoesNotExist
                                      description: |-
                                        operator represents a key's relationship to a set of values.
                                        Valid operators: In, NotIn, Exists, DoesNotExist.
                                      type: string
                                    values:
                                      description: |-
                                        values is an array of string values.
                                        Required for In and NotIn operators.
                                        Must be empty for Exists and DoesNotExist.
                                      items:
                                        type: string
                                      type: array
                                  required:
                                  - key
                                  - operator
                                  type: object
                                type: array
                              matchLabels:
                                additionalProperties:
                                  type: string
                                description: |-
                                  matchLabels matches namespaces with ALL specified labels (AND semantics).
                                  Example: {"environment": "production", "team": "sre"}
                                type: object
                            type: object
                          type: array
                      type: object
                    resourceNames:
                      description: resourceNames are specific resource object names
                        (supports wildcards). If empty matches any.
                      items:
                        type: string
                      type: array
                    resources:
                      description: resources names (plural). Use "*" for wildcard.
                        Subresources are matched via subresource field.
                      items:
                        type: string
                      type: array
                    subresources:
                      description: subresources (e.g. status). If empty matches none
                        (only main resource). Use "*" for any.
                      items:
                        type: string
                      type: array
                    verbs:
                      description: verbs like get, list, watch, create, update, patch,
                        delete, deletecollection
                      items:
                        type: string
                      type: array
                  required:
                  - apiGroups
                  - resources
                  - verbs
                  type: object
                type: array
            type: object
            x-kubernetes-validations:
            - message: at least one deny rule or podSecurityRules must be specified
              rule: size(self.rules) > 0 || has(self.podSecurityRules)
          status:
            description: DenyPolicyStatus holds policy evaluation state tracked via
              conditions.
            properties:
              conditions:
                description: |-
                  Conditions track DenyPolicy state (compilation status, validation errors, etc.)
                  All status information is conveyed through conditions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed DenyPolicy
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
