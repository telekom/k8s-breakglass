apiVersion: v1
kind: Service
metadata:
  name: webhook-service
  namespace: breakglass-system
  labels:
    app: breakglass
spec:
  ports:
    - port: 443
      targetPort: 9443
      protocol: TCP
      name: webhook
  selector:
    app: breakglass
  type: ClusterIP
