---
# Webhook service for admission webhooks
apiVersion: v1
kind: Service
metadata:
  labels:
    app: breakglass
  name: webhook-service
  namespace: system
spec:
  ports:
    - name: webhook
      port: 443
      protocol: TCP
      targetPort: 9443
  selector:
    app: breakglass
  type: ClusterIP
