version: "2"

run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  enable:
    - govet
    - ineffassign
    - misspell
    - errcheck
    - staticcheck
    - unused

formatters:
  enable:
    - gofmt
    - goimports
