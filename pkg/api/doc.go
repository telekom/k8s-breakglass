// Package api implements the main HTTP API server (Gin-based) for the breakglass
// controller, providing REST endpoints for escalations, sessions, cluster
// configs, OIDC proxy, SPA serving, and JWT-based multi-IDP authentication.
package api
