// Package config handles server-side configuration loading from YAML files and
// Kubernetes resources, including reconcilers for ClusterConfig, IdentityProvider,
// MailProvider, Escalation, DenyPolicy, and DebugPodTemplate CRDs.
package config
